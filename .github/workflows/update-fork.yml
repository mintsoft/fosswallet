name: Update Fork

on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'

jobs:
  update-fork:
    runs-on: ubuntu-latest
    steps:
      - uses: Shurtu-gal/autoupdate-fork-action@v1
        with:
          github_token: ${{ secrets.BOT_TOKEN }}
          base_branch: main
